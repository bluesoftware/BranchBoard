/**
 * Deprecated: the task modal was replaced by the right-side TaskDrawer.
 * Kept as a re-export for backward compatibility; prefer importing TaskDrawer.
 */
export { TaskDrawer as TaskModal } from "./TaskDrawer";
