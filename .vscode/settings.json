{
  // ===== BranchBoard – konfiguracja dla projektu "Leniwiec" (Git przez SSH) =====

  // Nazwa / tytuł tablicy
  "branchBoard.projectName": "Topestetic.pl",
  "branchBoard.boardTitle": "Topestetic.pl",

  // Dane lokalnie w repo (możesz commitować, by dzielić tablicę z zespołem)
  "branchBoard.storageMode": "workspace-json",
  "branchBoard.localDataFile": ".branchboard/board.json",

  // Tryb server = wspólna baza SQLite przez sqlite3 (NIE URL).
  //
  // sshHost PUSTY  -> wtyczka działa NA serwerze, baza otwierana lokalnie (bez SSH).
  // sshHost USTAWIONY ("user@host" lub alias z ~/.ssh/config) -> dostęp z innego
  //         komputera przez SSH do tej samej ścieżki sqliteRemotePath.
  //
  // Ten plik leży na serwerze, więc host zostaje pusty (dostęp lokalny):
  "branchBoard.sshHost": "",
  "branchBoard.sshPort": 22,
  "branchBoard.sqliteRemotePath": "~/sqlite/branchboard.db",

  // Pola legacy (nieużywane w trybie SSH) – zostają puste
  "branchBoard.serverUrl": "",
  "branchBoard.authToken": "",

  // Git
  "branchBoard.defaultMainBranch": "main",
  "branchBoard.remoteName": "origin",

  // Klucz SSH używany przez Git (i przyszłe połączenie z serwerem).
  // Pusty = domyślny agent SSH / ~/.ssh/config.
  // Najwygodniej: Cmd+Shift+P → "BranchBoard: Select SSH Key" (lista z ~/.ssh).
  "branchBoard.sshKeyPath": "",

  // Użytkownik wykrywany z `git config user.name` / `user.email`
  "branchBoard.autoDetectGitUser": true,
  "branchBoard.currentUser": "darek",

  // Odświeżanie / sync
  "branchBoard.syncIntervalSeconds": 20,

  // ===== Akcje Git sterowane kolumnami (pełna automatyzacja) =====
  // Przeniesienie karty do kolumny wykonuje akcję Git wg etapu kolumny:
  //   W trakcie   -> utwórz/checkout brancha feature/<id>-<slug>
  //   Code review -> push brancha
  //   Do testu    -> merge brancha do dev + push
  //   Zrobione    -> finish flow (push + merge do main + sprzątanie)
  "branchBoard.runGitActionsOnMove": true,
  // Pełny hands-off: bez okien potwierdzenia przy merge na przenoszeniu
  "branchBoard.confirmGitActionsOnMove": false,

  // ===== Bezpieczeństwo i flow "Zakończ zadanie" =====
  // Pełny, wygodny flow solo: Finish task pushuje, merge'uje do main i sprząta branch
  "branchBoard.allowDirectMergeToMain": true,
  // Pełna automatyzacja: nie pytaj przed merge do main
  "branchBoard.requireConfirmationBeforeMerge": false,
  // Nie zamknie zadania, jeśli masz niezacommitowane zmiany (zostawiamy jako bezpiecznik)
  "branchBoard.requireCleanWorkingTreeBeforeFinish": true,
  // Opcjonalna komenda przed zakończeniem (np. build) – pusta = pomijana
  "branchBoard.runCommandBeforeFinish": "",

  // Sprzątanie brancha po UDANYM merge (działa też po SSH)
  "branchBoard.deleteLocalBranchAfterMerge": true,
  "branchBoard.deleteRemoteBranchAfterMerge": true,
  "branchBoard.appearance.compactMode": false,
  "branchBoard.aiPromptTemplate": "",
  "git.autofetch": true,
  "git.autofetchPeriod": 10
}
